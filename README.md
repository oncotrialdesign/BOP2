# BOP2

What is included:

User-friendly wrappers:
  stopping_boundary_binary()
  get_oc_binary()
  search_binary_design()

A step-by-step example at: inst/examples/step_by_step_binary_example.R

A README with installation and example workflow.

Download the file bop2_0.1.0.tar.gz, then run the following code at R console.

# To Install
install.packages("C:\\External\\Manuscript - BOP2 Tree\\bop2_0.1.0.tar.gz", repos = NULL, type = "source")

library(bop2)

# To run the included example

example_file <- system.file("examples", "step_by_step_binary_example.R", package = "bop2")

source(example_file)


